#include <stdio.h>
#include <string.h>
#include "projeto1.h"

int global_flight_amount = 0;
flight global_flt_list[MAX_FLT];
flight global_sorted_flt_list[MAX_FLT];
char global_flt_code_list[MAX_FLT][FLIGHT_CODE_LENGTH];

add_flight(flight new_f) {
		
}
